// Shared layout constants — kept in one place to avoid magic numbers in multiple files
export const TAB_BAR_HEIGHT = 56
// 2px accent bar + 48px header content row
export const HEADER_HEIGHT = 50
// Secondary bar below header, visible only on the Map tab (Live/History toggle)
export const MAP_SUBHEADER_HEIGHT = 40
